class Solution(object):

    def rotateArray(self,nums,start, end):
        while(start<end):
            temp=nums[start]
            nums[start]=nums[end]
            nums[end]=temp

            start+=1
            end-=1






    def rotate(self, nums, k):
        n=len(nums)
        k=k%n
        self.rotateArray(nums,0,n-1)
        self.rotateArray(nums,0,k-1)
        self.rotateArray(nums,k,n-1)


        return nums
        
        
