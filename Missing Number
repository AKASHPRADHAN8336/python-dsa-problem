class Solution(object):
    def missingNumber(self, nums):
        n=len(nums)
        sum=0

        for i in range(0,n):
            sum+=nums[i]

        total_sum=(n*(n+1)/2)


        return total_sum-sum
