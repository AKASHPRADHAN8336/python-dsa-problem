class Solution(object):
    def maxSubArray(self, nums):
        max_sum=nums[0]
        curr=0
        for i in range(len(nums)):
            curr+=nums[i]
            if curr>max_sum:
                max_sum=curr
            if curr<0:
                curr=0

        return max_sum




 
