class Solution(object):
    def check(self, nums):
        n=0
        for i in range(len(nums)-1):
            if nums[i]>nums[i+1]:
                n=n+1

        if (n==1 and nums[0]>=nums[len(nums)-1]) or n==0:
            return True
        else: return False
