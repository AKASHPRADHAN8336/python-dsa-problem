class Solution:
    def largest(self, arr):
        # code here
        if len(arr) == 1:
            return arr[0];
        
        a=arr[0]
        for i in range(1,len(arr)):
            if arr[i]>a:
                a=arr[i]
                
        return a;
        
